using System;
using System.Collections.Generic;
using System.Text;

namespace Stacks
{
    class Stack
    {
       

        public Stack()
        {
            //Initializes the stack
        }

        private void chooseAction()
        {
            //Choose action, Move left, right, or forward
        }

        private Node initializeNode()
        {
            //Initializes the node
        }

        private Node popNode()
        {
            //Pop nodes unless stack is empty
        }

        private void seeStack()
        {
            //Displays what's in the stack
        }
    }
}
